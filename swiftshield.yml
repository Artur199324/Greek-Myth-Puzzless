excluded:
  # Исключаем классы, используемые в Storyboard
  # - '^AppDelegate$'
  - '^SceneDelegate$'
  - '^LoginViewController$'
  - '^MainViewController$'
  - '^SettingsViewController$'

  # Исключаем классы и файлы, относящиеся к OneSignalNotificationServiceExtension
  - '^OneSignalNotificationServiceExtension$'
  - '^.*OneSignal.*$'  # Если нужно игнорировать все классы, начинающиеся на OneSignal

  # Исключаем протоколы и делегаты
  - '^.*Delegate$'
  - '^.*DataSource$'

  # Исключаем методы, вызываемые через селекторы
  - 'application(_:didFinishLaunchingWithOptions:)'
  - 'buttonTapped:'

  # Исключаем классы, используемые в рефлексии
  - 'DynamicClass'
